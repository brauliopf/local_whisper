{
  "name": "@local-whisper/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --env-file=.env --import=tsx/esm src/server.ts",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.0",
    "fastify": "^5.0.0",
    "openai": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
